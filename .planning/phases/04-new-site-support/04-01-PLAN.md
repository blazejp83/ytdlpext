---
phase: 04-new-site-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [extension/manifest.json, extension/service-worker.js]
autonomous: true
---

<objective>
Add ok.ru, CDA (cda.pl), xhamster, and redgifs as supported sites in the extension.

Purpose: Expand site coverage for v1.1. All 4 sites are already supported by yt-dlp — the work is purely extension-side URL matching. New sites are popup-only (no injected download buttons).
Output: Extension recognizes the 4 new sites, shows badge, and enables popup downloads.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@extension/manifest.json
@extension/service-worker.js
@extension/content.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4 new sites to manifest and service worker</name>
  <files>extension/manifest.json, extension/service-worker.js</files>
  <action>
  1. In manifest.json, add URL match patterns to the content_scripts.matches array:
     - `"*://*.ok.ru/*"` and `"*://ok.ru/*"` (bare domain is common for short TLDs)
     - `"*://*.cda.pl/*"` and `"*://cda.pl/*"` (same reason)
     - `"*://*.xhamster.com/*"` (main domain)
     - `"*://*.xhamster2.com/*"`, `"*://*.xhamster3.com/*"` (common mirror domains)
     - `"*://*.redgifs.com/*"`

  2. In service-worker.js, add hostnames to the SUPPORTED_HOSTS array:
     - `"ok.ru"`
     - `"cda.pl"`
     - `"xhamster.com"`, `"xhamster2.com"`, `"xhamster3.com"`
     - `"redgifs.com"`

  No changes to content.js — new sites are popup-only (no injected buttons). The content script will still run on these sites for pageDetected notifications, and the isSupportedUrl fallback in getPageInfo handles cases where the content script hasn't fired yet.
  </action>
  <verify>
  - Grep manifest.json for ok.ru, cda.pl, xhamster, redgifs — all present
  - Grep service-worker.js for same hostnames — all present
  - Count entries in SUPPORTED_HOSTS — should be 11 (5 original + 6 new including xhamster variants)
  </verify>
  <done>All 4 sites (6 hostnames including xhamster mirrors) in both manifest.json and service-worker.js</done>
</task>

<task type="auto">
  <name>Task 2: Verify extension builds and site detection logic</name>
  <files>extension/manifest.json, extension/service-worker.js</files>
  <action>
  1. Validate manifest.json is valid JSON (parse it).
  2. Verify isSupportedUrl in service-worker.js correctly matches all new domains:
     - Test that ok.ru, www.ok.ru, m.ok.ru match
     - Test that cda.pl, www.cda.pl match
     - Test that xhamster.com, xhamster2.com, xhamster3.com match
     - Test that redgifs.com, www.redgifs.com match
  3. Verify no duplicate entries exist in SUPPORTED_HOSTS or manifest matches.
  </action>
  <verify>
  - `node -e "JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8')); console.log('valid')"` prints "valid"
  - Manual inspection: content_scripts.matches count increased, SUPPORTED_HOSTS count increased
  </verify>
  <done>manifest.json is valid JSON, all new sites pass isSupportedUrl matching, no duplicates</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] manifest.json is valid JSON
- [ ] All 4 new sites appear in content_scripts.matches
- [ ] All new hostnames appear in SUPPORTED_HOSTS
- [ ] No existing site entries were modified or removed
- [ ] No duplicate entries
</verification>

<success_criteria>

- All 4 sites added to both manifest.json and service-worker.js
- Extension manifest is valid JSON
- No regressions to existing 5 sites
- isSupportedUrl correctly matches all new domains
</success_criteria>

<output>
After completion, create `.planning/phases/04-new-site-support/04-01-SUMMARY.md`
</output>
